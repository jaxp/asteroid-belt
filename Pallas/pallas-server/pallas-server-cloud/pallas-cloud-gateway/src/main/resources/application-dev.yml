pls:
  auth:
    expire-time: 1000
    refresh-time: 180
spring:
  redis:
    host: localhost
    lettuce:
      pool:
        max-active: 8
        min-idle: 0
        max-idle: 8
        max-wait: 30
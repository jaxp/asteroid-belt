import { GeometricElfDirective } from './geometric-elf.directive';

describe('GeometricElfDirective', () => {
  it('should create an instance', () => {
    const directive = new GeometricElfDirective(null);
    expect(directive).toBeTruthy();
  });
});
